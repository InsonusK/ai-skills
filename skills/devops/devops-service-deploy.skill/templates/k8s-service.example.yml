apiVersion: v1
kind: Service
metadata:
  name: {service-name}
  namespace: {namespace}
spec:
  selector:
    app: {service-name}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
