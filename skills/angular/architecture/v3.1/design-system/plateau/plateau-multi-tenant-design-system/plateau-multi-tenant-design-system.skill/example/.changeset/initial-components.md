---
'design-system': minor
---

Add `DsButtonComponent` (signal API, Material-encapsulating) and `DsStatusChipComponent`
(fully custom, `--ds-color-status-*` tokens), plus the `theme.scss` / `custom-tokens.scss`
style entry points.
