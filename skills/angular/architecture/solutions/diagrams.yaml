cache_dir: .cache/diagram-renderer

tasks:
  - id: angular-solutions-overview
    source:
      include:
        - "skills/angular/architecture/solutions/**/solution-*.skill.md"
    metadata:
      subpath: "# Capabilities"
    links:
      - name: depends_on
        type: frontmatter_field
        field: depends_on
        reverse: true
        transitive_reduction: true
    output:
      format: obsidian_canvas
      destination: "skills/angular/architecture/solutions/angular-solutions-overview.canvas"